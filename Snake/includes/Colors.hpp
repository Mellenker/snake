#pragma once

#include <SFML/Graphics/Color.hpp>

namespace Colors {
	const sf::Color grassDark(0, 132, 9);
	const sf::Color grassLight(0, 118, 9);
	const sf::Color appleColor(255, 0, 0);
	const sf::Color snakeHead(103, 0, 255);
	const sf::Color snakeTail(143, 0, 204);
}
